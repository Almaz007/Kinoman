import styles from './cinema.module.css';

const Cinema = () => {
	return (
		<div className={styles['cinema']}>
			<h2 className={styles['title']}>Кинотеатр</h2>
		</div>
	);
};

export default Cinema;
